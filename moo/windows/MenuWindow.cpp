#include "MenuWindow.h"

namespace windows
{
#if 0
   MenuWindow::MenuWindow(WindowContext context, const graphics::FontMetrics& font, ColorScheme scheme)
      : WindowContext(context)
      , m_font(font)
      , m_scheme(scheme)
   {

   }

   void MenuWindow::AddItem(const ItemData& data, std::function<void()> onSelected)
   {

   }

   MenuItem::MenuItem(WindowContext context, const std::u8string& text, std::function<void()> onSelected)
   {
      context.graphics->CreateTexture
   }
#endif
}